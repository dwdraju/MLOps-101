[project]
name = "company-info"
version = "0.1.0"
description = "MCP server for company info with csv"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.11.3",
    "pydantic>=2.11.7",
    "requests>=2.32.5",
]