version: '3'
services:
  jupyter:
    build: .
    command: start-notebook.py --NotebookApp.token='my-token'
    ports:
      - "8889:8888"
    volumes:
      - ./jupyter-data:/home/jovyan/work
    restart: always
